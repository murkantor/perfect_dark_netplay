# Perfect Dark Port — Claude Reference

## Environment Rules (READ FIRST)

**Building is allowed — use the MSYS2 MinGW x64 shell at `C:\msys64`.** The repo only compiles inside that shell; invoke it from PowerShell like this:

```powershell
$env:MSYSTEM='MINGW64'; $env:CHERE_INVOKING='1'
& C:\msys64\usr\bin\bash.exe -lc 'cd "/c/Users/tidbu/source/repos/_remote/perfect_dark_netplay/build_debug_sdl3" && cmake --build . -j 2>&1 | tail -40'
```

- `MSYSTEM=MINGW64` selects the MinGW x64 toolchain (`/mingw64/bin`); `CHERE_INVOKING=1` keeps the login shell from cd-ing to home. Use MSYS path form (`/c/Users/...`) inside the bash command string.
- `build_debug_sdl3/` is the primary configured build dir (SDL3 + SDL_GPU). Configure fresh dirs with `cmake -G "Unix Makefiles" [options] ..` from the same shell.
- Builds can take minutes — use a long timeout or run in the background and tail the output.
- **Do not run the built game** (`pd.*.exe`): it needs a ROM and opens a window. The user runs and reports back.

## Code Writing Guidelines

- **Do not rename decompiled symbols.** Identifiers under `src/` map to the original N64 binary; renaming silently breaks the decompilation contract.
- Keep changes minimal and focused on the task at hand — no surrounding cleanup, no speculative abstractions, no refactor-while-you're-there.
- Respect existing patterns. If you're adding netplay-aware code, use the guard patterns documented in `src/game/CLAUDE.md` (`#ifndef PLATFORM_N64`, `g_NetMode != NETMODE_CLIENT`).
- Take notes as you work and re-read changes before reporting done — this catches scope creep and broken assumptions early.

## Repository Overview

Work-in-progress port of the [Perfect Dark N64 decompilation](https://github.com/n64decomp/perfect_dark) to modern platforms (Windows, Linux, macOS, Nintendo Switch). The source ROM must be supplied separately.

- **Main branch**: `port` — stable port, no netplay.
- **Netplay branch**: `port-net` (`remotes/origin/port-net`) — experimental internet/LAN multiplayer.
- **Local netplay-predict branch**: `port-net-predict` — adds CSP, entity interpolation, and lag compensation on top of `port-net`. Forked at https://github.com/murkantor/perfect_dark_netplay.

## Repo Layout

```
CMakeLists.txt          — build system entry point
src/game/               — original N64 game logic (decompiled C)
src/include/            — game headers (types, constants, bss, data)
src/lib/                — original N64 lib code (sched, vi, snd, etc.)
port/src/               — port-specific platform layer
port/include/           — port headers
port/fast3d/            — libultraship fast3d renderer (OpenGL)
port/external/          — bundled third-party libs (minimp3, enet on port-net)
docs/                   — supplemental docs (netplay.md, etc.)
tools/                  — helper scripts
```

## Build System

CMake with `Unix Makefiles`. Does NOT support Visual Studio. Key flags:
- `-DROMID=pal-final` / `-DROMID=jpn-final` for non-NTSC builds
- Target executable: `build/pd.<arch>[.exe]`

**Toolchain (Windows): MSYS2.** Install dependencies once from an MSYS2 shell:

```
pacman -S mingw-w64-x86_64-toolchain mingw-w64-x86_64-sdl3 mingw-w64-x86_64-zlib mingw-w64-x86_64-cmake mingw-w64-x86_64-python3 mingw-w64-i686-toolchain mingw-w64-i686-sdl3 mingw-w64-i686-zlib mingw-w64-i686-cmake mingw-w64-i686-python3 make git
```

Build from the **MSYS2 MinGW x64** shell (not the plain MSYS2 shell). If the exe launches with `0xc000007b`, check DLL bitness — a 32-bit DLL next to a 64-bit exe is the usual cause.

## Netplay (high level)

Client-server over [ENet](http://enet.bespin.org) reliable UDP — the host is always the server, up to `MAX_PLAYERS` (8) clients including the host itself. Default port `27100`, configurable via `pd.ini` or `--port`. Protocol version constant lives in `port/include/net/net.h` (`NET_PROTOCOL_VER`) and must be bumped whenever the wire format changes. Status: highly experimental, only Combat Sim is partially functional. For protocol, CSP, interpolation, and lag-comp design, see `port/src/net/CLAUDE.md`.

## How to find things

**Grep first, read second.** This codebase is ~half a million lines of decompiled C — speculative reading is wasteful. Common symbol prefixes to grep for:

- `g_*` — globals (game state, config, BSS-resident tables). Examples: `g_Vars`, `g_NetMode`, `g_BotConfigsArray`, `g_NetLocalClient`.
- `net*` — netplay functions and structs in `port/src/net/` and `port/include/net/`.
- `SVC_*` — server→client message IDs (declared in `port/include/net/netmsg.h`).
- `CLC_*` — client→server message IDs (same file).
- `UCMD_*` — input bitflags on `netplayermove.ucmd` (`net.h`). `UCMD_FL_FORCE*` are server force-correction bits.
- `CLSTATE_*` — client connection-state enum (`net.h`).
- `NETMODE_*` — `NONE` / `SERVER` / `CLIENT`. The host has `NETMODE_SERVER`; gate server-only writes on `g_NetMode != NETMODE_CLIENT`.

For per-file purpose lookups (which `.c` does what), consult `docs/CODEMAP.md` before opening files at random.

## Nested CLAUDE.md and docs index

### Auto-loaded CLAUDE.md (loaded when Claude Code operates under the matching directory)

- `port/src/CLAUDE.md` — Port platform-layer file index (`main.c`, `pdsched.c`, `video.c`, `console.c`, …) and console scrollback behaviour. Loads when editing anything under `port/src/`.
- `port/src/net/CLAUDE.md` — Full netplay protocol (SVC_*/CLC_* tables, player-move struct, prop sync), CSP / entity-interpolation / lag-comp design notes, debug console commands, F9 overlay. Loads when editing anything under `port/src/net/`.
- `port/include/net/CLAUDE.md` — Net header inventory; field-ordering and protocol-version gotchas (`netClientNeedMove` memcmp exclusion, `NET_CSP_*` macro aliases). Loads when editing net headers.
- `src/game/CLAUDE.md` — Net-guard patterns specific to decompiled game logic (`#ifndef PLATFORM_N64`, `g_NetMode != NETMODE_CLIENT`); positional weapon sounds design. Loads when editing anything under `src/game/`.
- `src/game/mplayer/CLAUDE.md` — Multiplayer subsystem: deterministic bot allocation invariant, `g_BotConfigsArray` wire sync, `actiontype` non-sync gotcha. Loads when editing MP setup / scenarios / scoring.
- `src/include/CLAUDE.md` — Shared headers; flags the five port-only additions to `constants.h` (`MPOPTION_NOCULL`, `CHEAT_NOCULL`, …) and the cheat-index append-only rule. Loads when editing shared headers.

### Load-on-demand docs (read when the situation matches)

- `docs/CODEMAP.md` — One-line purpose per non-obvious `.c` / `.h` under `src/` and `port/`. Read when you need to locate which file does X before opening anything.
- `docs/PORT_NET_PREDICT_CHANGES.md` — Per-file rationale for every change on `port-net-predict`. Read when you need to understand *why* a file was modified, not just what changed.
- `docs/PORT_NET_KNOWN_ISSUES.md` — Current netplay limitations and partially-broken features. Read before promising any feature works on clients, or when diagnosing client-only bugs.
- `docs/PORT_NET_SOAK.md` — Phase 2 prop-sync **soak harness**: the in-engine invariant auditor (`netprop.c` `netPropAudit`, `/audit [on|off|now|rate N]`) that runs once a second on both roles, re-derives the prop-sync invariants by **pool iteration** (cycle-immune — never walks `->next`), and emits a machine-parseable `audit:` line (dupes/corpses/orphan/overcap standing-corruption + heal/reap/orphreap fire counters + an order-independent `manifest` digest of the networked syncid set); the offline verdict tool `tools/netsoak.py` (single-log invariant check + server↔client manifest-parity, exit 0/1, unit-tested on synthetic logs); and `tools/soak/` (churn-max playlist + `run_server.sh` + `run_client.sh`). The fire counters (`g_NetAudit*Fires`) are bumped at the `propsheal`/`proptick_guard`/`orphan_reap` sites. Includes a **headless client** (`--headless-client <addr>`, main.c + net.c): the dedicated build's headless runtime but JOINs instead of hosts — the prop-sync apply + auditor run in the tick path (lvRender skipped), so server+client can both be headless for an unattended two-process soak. Honest limits: round-boundary respawn only (client spawn is server-authoritative via `mpStartMatch`; the lv.c `playerStartNewLife` is render-gated off for clients), neutral input (stationary pawn), and the local-pawn-headless render-prep seam is **compile-verified but not runtime-proven**. Read when running a soak, diagnosing an `AUDIT FAIL/WARN` line, or extending the auditor/headless client. **Needs a ROM to run end-to-end.**
- `docs/PORT_NET_PROP_LIFECYCLE.md` — The 2026-06-10 netprop consolidation (`port/src/net/netprop.c`): `netPropFreeSynced` teardown choke point (all port-added weapon/obj frees + post-free slot tripwire), `netSyncPropSpawn` single spawn-broadcast helper (uniform gates + autogun owner guard + same-syncid dedupe — fixed the objDrop+caller DOUBLE-SPAWN ghost-gun bug), read-side one-prop-per-syncid (latest spawn wins), client "wire owns the lifetime" gates (propExplode + hard-free), the dedicated-server hard-free budget, Combat Sim client pickups (request + echo-give), the syncid diet (ids assigned at activate/pause, explosions/smoke excluded; reconcile cap 4096→65536), and the lifecycle event ring behind `/proplog`. **Compile-verified only — run its runtime test checklist before trusting.** Read when touching prop spawn/free paths, `netprop.c`, or before hunting any new prop corruption (start with `/proplog`).
- `docs/PORT_NET_PROP_SYNC_CATALOG.md` — Catalog of every networked prop class (deterministic stage props vs runtime-dynamic), its SVC_PROP_* sync coverage, and its GC/lifecycle status, cross-referenced to live symptoms (Combat-Sim clients can't pick up dropped weapons = `objTestForPickup` coop-only gate at propobj.c:17808; floating guns = no wire-pos reconcile for WEAPON props; laptop sentry = server-only fire AI). Documents the prop-list invariant the crash family violates (free-without-delist / re-activate → null-obj corpse + `->next` cycle) and a GC checklist for new dynamic prop types. Read when touching dropped-weapon/projectile/mine/sentry sync, `objTestForPickup`, the `SVC_PROP_SPAWN`/`_MOVE`/`_FREE`/`_RECONCILE` paths, or the physics-weapon (mine/dragon/devastator/grenade) lifecycle.
- `docs/PORT_NET_CRASH_LEDGER.md` — Chronological ledger of every client crash/hang hit testing the master client against a VPS dedicated Combat Sim (site/fault/root/fix/status table + symbolication recipe). Documents the two failure families — (A) prop-list corruption (cycles + null-union corpses in activeprops/chr-child chains, from the dual-use `->next`) and (B) `weaponCreate` force-recycle free crashes when the 50 weapon slots fill — and the single systemic condition feeding both: the client running BEHIND the dedicated server's high-churn prop/weapon stream (−44 drift, sub-60fps). Read when a NEW client crash appears in hosted play (append to it), or to understand why the heal/reap/guards exist and what the remaining live corruption generators are.
- `docs/PORT_NET_REVERTED_EXPERIMENTS.md` — Failed approaches with root-cause analysis. Read before re-attempting anything that smells like a previously-tried idea.
- `docs/PORT_SHINY_ALPHA.md` — Fix for the "shiny surfaces see-through in dark rooms" family (the old Extraction-NVG regression): the dlights.c reshade scales the env/shiny vertex class's (`vtx.flags & 0x01`) palette ALPHA by room brightness → 0 in blackouts → SHADE_ALPHA-blended surfaces vanish. Fix = `bgBuildShinyXluMask` (bg.c, per-room bitmask of colour entries referenced by XLU-layer geometry) + a floor in the reshade pinning only opa-layer (metal) entries at `Video.ShinyAlphaFloor` (default 255); glass keeps the vanilla fade. `/shinyalpha [N|info]`. Read when touching the dlights.c reshade, room gdl parsing (`G_COL`/`bgGetNextGdlInLayer`), or any surface that goes transparent as lights dim.
- `docs/PORT_CRASH_SYMBOLS.md` — Crash symbolication workflow: the `BUILD: <branch> <hash> (<target>) link=<PE-stamp>` line in every crash dump (crash.c, both platforms; `link=` is the per-link PE timestamp — the authoritative match key, since the git hash is configure-time and goes stale), the gitignored `symbols/` archive of per-shipped-build `.debug` files (`objcopy --only-keep-debug`), and `tools/symbolicate.py` (`<log>` to symbolize with auto archive-matching; `--archive <exe>` at every deploy). Read when symbolicating a user crash report, touching crash.c's dump format, or changing the deploy routine.
- `docs/PORT_HEADLESS_BLIND_SERVER.md` — **The blind-server compendium**: the five blindness axes (no render pass / no render-populated state / no local pawn / no platform subsystems / can't simulate remote weapons), the cut "on-screen" pipeline chain (`ROOMFLAG_ONSCREEN` → `func0f08e8ac` → `needsupdate`/`pass2` → `PROPFLAG_ONTHISSCREENTHISTICK` → empty `onscreenprops` + no chr matrices), the full catalog of implemented workarounds (mainTick mirror, `alwaystick`, interact active-list scan, identity world-to-screen, orphan/slot-0/spectator families), and the gap ledger: `handsTickAttack` mirror **implemented** (§6.1 — remote grenade/rocket/mine spawns now run server-side, gated on the Tier 2 camera prime; inherits the listen-host prop double-apply quirk, fix direction documented), blind hit-validation (closed by §9 + §6.1, runtime-unproven), screen-gated frees (closed by §9 Tier 1), `explosionCreate` cam_pos, co-op revive health-steal (open). §9 is the systemic fix, **implemented (compile-verified, runtime-unproven)**: two-tier visibility restoration — Tier 1 forces the `func0f08e8ac`/`posIsInDrawDistance` predicates true on the blind server (sim always runs: matrices, frees, traces), Tier 2 builds real per-combatant camera matrices + runs `bgTickPortals` in the pdmain.c headless loop, deriving `g_MpRoomVisibility` from the already-sent view pose (spawn avoidance, AI LOD, per-shooter validation; derive-never-trust). Ends with the 8-rule checklist for new code. Read BEFORE touching the pdmain.c headless block, anything gated on `g_NetDedicatedMode`, lvRender's per-player loop, or any "is this on screen" logic that a server might execute.
- `docs/PORT_NO_CULLING.md` — **RETIRED** "No Room Culling" / "No Draw Slot Limit" MP option + cheat feature (superseded by `/octree bigroom`; cheat indices 43/44 + MP bits 28/29 reserved). The `g_BgNoCull`/`g_BgNoDrawSlotLimit` globals and the grown draw-slot table remain — read this doc's mechanics section when touching `bg.c` draw-slot logic.
- `docs/PORT_GOLDENEYE.md` — Port-only "GoldenEye Style" master + **Classic Options breakdown**: the 12 GE behaviours (snap lean, lower-and-raise reloads, ledge wall, classic crosshair, hide-unless-aiming, GE arc HUD + damage flash, no secondary functions, no mid-crouch, no dual-wield, i-frames + fire lockout, no crouch accuracy, no blur) each individually toggleable as `CHEAT_CLASSIC_*` 49-60 (Extended > Experiments > Classic Options) and `MPOPTION_CLASSIC_*` high-word bits 34-45 (Combat Sim "Classic Options" carousel page). Gate = master OR individual via `classicOptionActive(cheat, mpoption)` in cheats.c. Documents the reusable `bgunSecondaryFunctionDisabled` / `bgunDualWieldDisabled` helpers that future weapon-loadout work plugs into. Read when touching any GE gate site, the Experiments menus, or the scenario-options carousel.
- `docs/PORT_OFFLINE_32_SIMS.md` — Port-only **offline 32 simulants** + the 4-page Simulants carousel (slots 1-8/9-16/17-24/25-32, the Classic-Options sibling pattern; pages 2-4 `MENUDIALOGFLAG_NETPLAY_HIDDEN`). chrslots is now **u64** (players 0-15, bots 16-47 — ALWAYS use `MPCHRSLOT(n)`, plain `1 <<` is UB past bit 31); `NET_MAX_BOTS 8` caps every wire/lobby/query surface (no proto bump — the `setupCreateProps` RNG-parity clamp is LOAD-BEARING for cross-build determinism) and `MAX_BOTS_PRESET 8` pins every file image (ROM mpconfig, wad bot block — a serialization loop must never use `MAX_BOTS`). Extras are session-only (saved setups keep slots 1-8). Read when touching chrslots, bot-slot loops, the Simulants menu, or anything serializing bot configs.
- `docs/PORT_KOH_STATIC_HILL.md` — Port-only KotH "Static Hill" dropdown (per-stage hill picker) + Mobile/Static mode dropdown. Read when touching `kingofthehill.inc` hill selection, `mpsetup` versioning, or `SVC_STAGE_START` payload ordering.
- `docs/PORT_CTC_STATIC_BASE.md` — Port-only CTC per-team base pins (four "Team N Base" dropdowns, dynamic enable based on player count). Read when touching `capturethecase.inc` team-assignment, `mpsetup.ctcteambase[4]`, or the menu handler patterns that share `item->param` across multiple entries.
- `docs/PORT_HTB_HTM_STATIC_SPAWN.md` — Port-only HTB / HTM "Static Spawn" dropdowns. Read when touching `holdthebriefcase.inc` `htbCreateToken`, `hackthatmac.inc` `htbCreateUplink` (which is the HTM function — naming quirk), or the 28-pad constant derived from intro-command counting.
- `docs/PORT_RACE.md` — Port-only "Race" Combat Sim scenario (`MPSCENARIO_RACE 8` — **the first live hi-bit scenario**: wad id rides the v10 tail high-bit + the `g_ScenarioSaveSlotRaw` capture, block now 100% FULL): checkpoint racing over the KoH hillpads in stage order (a lap = all N; "Laps" 1-10), first to finish wins but the match runs until all humans finish or the post-winner "Finish Timer" (0-120s) expires (`raceShouldEndMatch` polled from lv.c's match-end block); per-player next-checkpoint radar dot, all checkpoint rooms hill-green, synthetic ranking scores (score limits forced unlimited), bots don't race. `SVC_RACE_STATE` (0x58, proto 72). Read when touching the hi-bit wad scheme, `INTROCMD_HILL` parsing, or before adding a 10th scenario.
- `docs/PORT_ELIMINATION.md` — Port-only **global Lives system** (Limits menu: Lives 0=Off/1-9 + Solo/Team mode; formerly the short-lived "Elimination" scenario id 8, retired before release): the co-op F3 lives rules layered onto ANY scenario — death spends a life (`mpstatsRecordDeath` hook), out-of-lives respawn vetoed in `player.c` + `chrTickDead` (`elimChrCanRespawn`; bot corpses kept, never deleted), Team mode = one shared pool of exactly N, match ends via `elimShouldEndMatch` **polled from lv.c's match-end block** (`g_NumReasonsToEndMpMatch` resets each frame — never increment it from a scenario tick). State in standalone `g_ElimData` (NOT the `g_ScenarioData` union); hooks ride the scenario dispatchers in `scenarios.c`. `SVC_ELIM_STATE` (0x57, proto 71); mpsetups v9 tail (**1 spare bit left** — next field needs `MPSETUP_BLOCKSIZE` enlarged). Appendix documents the retired v8 scenario hi-bit scheme — read before adding a 9th scenario or touching respawn paths / the wad tail.
- `docs/PORT_GRAFFITI.md` — Port-only "Graffiti" Combat Sim scenario (`MPSCENARIO_PAINTROOM`, 7th scenario, team room-painting): living combatants tint the floor of the room they stand in their team's colour, re-paintable (last team to cross owns), most rooms at the time limit wins. Reuses the CTC room-highlight vtable (`scenarioIsRoomHighlighted`/`scenarioHighlightRoom` → `dlights.c`); host-authoritative ownership in `g_ScenarioData.paint.roomowner` (per-stage `MEMPOOL_STAGE` array) broadcast via reliable `SVC_PAINT_STATE` (0x55, proto 66 — full owned-room list, on-change + 1s heartbeat). Includes the `MPOPTION_OWNEDROOMSPAWN` (bit 33) respawn-into-owned-territory option (`paintChooseSpawnLocation` spawnfunc). Read when touching the scenario vtable (`scenarios.c` `g_MpScenarios[]`/`paintroom.inc`), the room-highlight/`roomSetLightOp` path, or `mpApplyLimits` / `menuhandlerMpTeamsEnabled` scenario gating.
- `docs/PORT_ZONES.md` — Port-only "Zones" Combat Sim scenario (`MPSCENARIO_ZONES`, 8th scenario — **the last free 3-bit scenario index**): TS2-style territory control on the KotH framework — every stage hillpad is an active zone at once, standing in a zone flips it to your team (optional Capture Time hold slider, KoH contested-pause), and a repeating Score Interval cycle awards each team +1 per zone owned; match ends on team score / time limit. Host-authoritative via reliable `SVC_ZONES_STATE` (0x56, proto 69 — zone owners + team scores + cycle countdown, on-change + 1s heartbeat); both sliders persist in the 32-bit per-scenario save slot (KoH `mphilltime`'s slot — no wad version bump). Read when touching the scenario vtable, `INTROCMD_HILL` parsing, the per-scenario save slot, or the `mpCalculateTeamScore`/`mpApplyLimits` scenario overrides.
- `docs/PORT_CHAOS.md` — Port-only **Chaos / randomiser mode** (Lua): `scripts/chaos.lua` fires a weighted random effect (~72: arsenal + ammo roulette (rockets/grenades/golden-gun rounds from any gun), timed cheat-bank toggles, self-destruct, yeet/boom/panic world chaos, untextured/watercolour/noir/room-tint/disco visual modes) every `interval` seconds, all via `pd.*` hooks so new effects are pure Lua. External event ingress = the Twitch/YouTube window: C ring queue (`luaExtEventPush` → `pd.ext_poll()`, luaai_api.c) fed by the `/chaos <text>` console command and a **localhost-only** UDP listener (`Chaos.EventPort`, default 0=off, bound 127.0.0.1 via `enet_address_set_ip` — never widen it). Text protocol `on/off/trigger <fx> [who]/vote <fx>/votetime N/interval N/seed N/say`; vote windows for chat (3-candidate HUD slate, top-right, with active-effect timer bars); `chaos.trigger()`/`chaos.set_seed()` exposed for the AP client (trap items/DeathLink, per-seed determinism). 43 new `pd.*` bindings (+device_off/lvupdate) (cheat/sound/weapon juggling/fade/chr_yeet/explosion/alarm/boost/dizzy/chr_cloak/strip_ammo/teleport_to_chr/flattex/grayscale/room_tint/ammo_swap/explosions_around/backfire/nbomb/gust/dual_wield/aspect_scale/fov_scale/song/spawn_body/body_snatch/chr_target/chr_calm/doors_all/chr_summon/ext_poll) backed by `chraiLua*` helpers in chraction.c + renderer globals in gfx_pc.cpp (`gfx_flattex_mode` upload-filter, `gfx_force_grayscale`) + the dlights.c stage-wide room tint. **Compile-verified only — run its runtime checklist.** Read when touching the chaos ingress, luaai_api bindings, or wiring a chat bot / AP traps to effects.
- `docs/PORT_VR.md` — Port-only **PCVR / OpenXR mode**: a verbatim port of [Alex-LeTux/perfect_dark_VR][vr] gated at COMPILE time on `PD_ENABLE_VR` (`cmake -DUSE_VR=ON`, forces `USE_SDLGPU=OFF`; OpenGL-only because stereo is one geometry pass through `GL_OVR_multiview2` into the layered OpenXR swapchain). Every hook is `#ifdef PD_ENABLE_VR <upstream verbatim> #else <our original> #endif`, so the flat exe compiles zero VR code and `port/vr/` leaves the source glob. Covers: the porting rule (copy upstream, never re-derive — its constants are hardware-tuned), the multiview shader prelude and its ONE sanctioned deviation (`mvPos = uMVP * aVtxPos`, composing with the dlcache), the whole-project **resolution regime** (`constants.h` SCREEN/FBALLOC doubling + `SCREEN_ASPECT 1.0f`, doubled `vi.c` VI math, `videoGetAspect`→`XrAspect`, eye-scale display modes) which must be taken whole or not at all, FOV-normalised sun/glare/spark sizing, the FOV-derived per-stage/cutscene **`vr_world_scale`** (the thing that makes VR feel right), the SDL3 mirror window replacing upstream's ImGui toolbar, and the netplay/splitscreen/shared-save analysis (flat exe structurally unaffected; four VR-exe sites where upstream's single-player assumptions would drive REMOTE pawns). **Compile-verified only — no headset has run it.** Read before touching `gfx_run`'s VR branch, the multiview code or rapi vtable tail, `mainTick`'s VR hooks, the resolution regime, or any `bgun*`/`bwalk*`/`bmove*` aiming/movement code.

[vr]: https://github.com/Alex-LeTux/perfect_dark_VR

- `docs/PORT_SDL3_EXTRAS.md` — Port-only SDL3-era extras: per-player gamepad RGB-LED colours with low-health flash (`Input.GamepadLED`, `inputUpdatePadLEDs`), impulse-trigger rumble mirror (`Input.PlayerN.TriggerRumble`, gated on gamepad capability properties), boot-time taskbar progress (`videoSetTaskbarProgress` / new `set_taskbar_progress` wmAPI entry, SDL 3.4+ only, guarded `SDL_VERSION_ATLEAST(3,4,0)`), and **gyro aim** (pad 1 gamepad gyro integrated by sensor timestamps in the event watcher, merged into `inputMouseGetScaledDelta`; `/gyro`, `Input.GyroAim`/`GyroSpeedX/Y`, default off). Read when touching `inputRumble`/`inputInitController`/`inputMouseGetScaledDelta`, the `GfxWindowManagerAPI` struct, or `romdataInit` boot preprocessing.
- `docs/PORT_SDLGPU.md` — Port-only SDL_GPU (Vulkan/D3D12/Metal) fast3d rendering backend (`port/fast3d/gfx_sdlgpu*.cpp`), selectable via Extended > Video / `Video.Renderer=sdlgpu` / `--renderer sdlgpu` + `--gpu-driver`; OpenGL stays default. Covers the backend seam (`gfx_sdl_set_backend` no-GL-context window mode), offscreen-fb0 + final-blit architecture, runtime GLSL450→glslang→SPIR-V shader pipeline (+SPIRV-Cross→DXBC/MSL for D3D12/Metal) + SDL_GPU descriptor-set conventions, pipeline/sampler caches, the dlcache/palette mapping, the disk shader cache, **HDR output** (scRGB/HDR10, paper-white + peak sliders, `G_SETDAZZLE_EXT` plumbing — currently inert), **VRR mode** (`Video.VSync=-2`, works on GL too), and the `/gpu` console command. Build deps: glslang (required for `USE_SDLGPU`, OFF for Switch/dedicated) + spirv-cross (optional, enables D3D12/Metal). Read when touching `gfx_sdlgpu*`, the `Video.Renderer` selection in `video.c`, or the `wm_use_gpu` guards in `gfx_sdl.cpp`. **Status: verified on Vulkan + D3D12 incl. dlcache/MSAA/HDR; Metal untested.**
- `docs/PORT_GUN_FOV.md` — Port-only "Gun FOV" slider (separate viewmodel FOV: the first-person gun/hands render at their own FOV so high Vert FOV doesn't warp the weapon) + tan-space relative weapon zoom (`ADJUST_ZOOM_FOV` → `playerAdjustZoomFovY`: zoom levels preserve true on-screen magnification at any base FOV). Six interlocking pieces: `bgunRender` projection override (`viPerspectiveFov`), NeonNyan offset rebase, aim-pose sync, laser-sight reprojection, zoom-tracking gun FOV, HUD X-readout inverse map. Read when touching `bgunRender`'s projection setup, `bgun0f0a5550` aim tracking, `bgunUpdateLasersight`, `ADJUST_ZOOM_FOV`/`gunzoomfovs`, or the `fovzoommult` plumbing. **No `tanf` in the codebase — see the doc's gotchas before adding FOV math.**
- `docs/PORT_WEAPON_PRESETS.md` — Port-only Custom Weapon Presets system (`mpsetups.bin` v2 tail; per-slot `FNFLAG_*` bits; `g_MpWeaponPresets[]`; `g_MpSlotFnFlags[]`, wire-synced in `SVC_STAGE_START` since proto 58). Read when touching the Combat Sim Weapons menu, the Random Preset rotation, or the `bgunPrimary/SecondaryFunctionDisabled` helpers.
- `docs/PORT_SERVER_WEAPON_BANS.md` — Port-only server weapon / weapon-function bans: playlist `[server]` `banned=` key (weapon names + `:pri`/`:sec` suffixes, `GADGETS`/`MINES` groups), enforced by `playlistApplyWeaponBans()` from `mpStartMatch`'s option-strip block (banned slots → Nothing; fn bans ride `g_MpSlotFnFlags`, synced at proto 58). Read when touching the playlist `[server]` parser, `mpStartMatch`'s server-gated block, or the `SVC_STAGE_START` weapons/fn-flag block.
- `docs/PORT_NODOORS.md` — Port-only "No Doors" Combat Sim option. Historically introduced the `g_MpSetup.portoptions` overflow word; that word was **later removed** when `g_MpSetup.options` was widened to `u64` (`MPOPTION_NODOORS` now = bit 32, `0x0000000100000000ULL`, tested against `options`). Read when touching `setupMarkLiftDoors`/the `OBJTYPE_DOOR` skip in `setup.c`, the high-word `menuhandlerMpCheckboxPortOption`, or how high-bit (32-63) MP options ride `options` in `SVC_STAGE_START` / `CLC_ADMIN_SETUP` / `SVC_LOBBY_STATE` and the mpsetups.bin wad. The doc's top callout summarises the merge; the body below it is the original portoptions design (historical).
- `docs/PORT_HOSTED_SERVER_FINDINGS.md` — The Host Online Game **debug findings catalog** (2026-06-08): the R4 net-clock-rate fix (`g_NetTick += diffframe60` — sub-60fps machines drifted unboundedly, teleporting everyone; affects ALL netplay), the five slot-0-assumption fixes (contpads/mouse×3/hudmsg — any dedicated-server client at slot≥1), stale one-shot input rebroadcast + ungated SELECT replay (anim loops), the drain-loop NULL-host crash, the playerReset uninitialized-rooms eternal hang, basename mod-dir auth, and the diagnostic playbook (netdiag truncation/sharing traps, gdb-attach procedure, diag event semantics). Read BEFORE touching g_NetTick consumers, bwalkUpdateRemote's snap paths, the outmove ucmd carry, or when a per-player feature breaks only for dedicated-server clients.
- `docs/PORT_HOSTED_SERVER.md` — Port-only "Host Online Game": the master server VPS spawns a dedicated instance on request (PDMS opcodes 0x06-0x08 HOST_REQUEST/GRANT/DENY — **no NET_PROTOCOL_VER bump**, master protocol only) and the requester auto-connects as an auto-admin client (`CLC_ADMIN login <token>` + `take`, reliable ordered channel, no reply parsing) driving the FULL Combat Sim hosting UI — entered via a fresh-CITRAINING reload + the post-match menu latch (`var80087260`, the documented-safe alternative to the shieldhits crash class), with "Begin Match" rerouted to `CLC_ADMIN_SETUP` at menutick.c's `-5` sentinel + a 10s push watchdog in net.c (`g_NetHostOnlinePushTick`). Master side: `pdmaster/instances.go` (spawn-on-demand, idempotent grants keyed on source IP, public-IP substitution for loopback heartbeats + flood-cap exemption, 2min never-connected / 5min empty reaper). Read when touching `netHostRequest*` (netmaster.c), menutick.c's post-match latch or `-5`/`-6` sentinels, any `g_NetHostOnlineMode` gate, or pdmaster.
- `docs/PORT_HOST_SPECTATOR.md` — Port-only Host Spectator Mode (`MPOPTION_HOSTSPECTATOR`, WIP/paused). Read when touching `port/src/spectator.c`, the `is_spectator` wire bytes in `SVC_STAGE_START` / `SVC_LOBBY_STATE`, or `playerGetLocalCount` / `LOCALPLAYERCOUNT()`. **Has known breakage with remote combatants — read the "Known breakage" section before extending.**
- `docs/PORT_CLIENT_SPECTATOR.md` — Port-only Client Spectator (render-redirect): a client/host renders a spectated **player's own viewport** (full first-person/HUD) by substituting the target's slot in `lvRender`'s per-player loop, plus on-death auto-spectate, `/spec toggle`, sim camera, and the `g_NetSpectateChr` dangling-pointer guards. Read when touching the `lvRender` per-player loop, `netSpectate*` in `net.c`, or `mainEndStage`. Contrast with the (broken) panel-based `SPEC_MODE_PLAYER` in `PORT_HOST_SPECTATOR.md`.
- `docs/PORT_WIREFRAME.md` — Port-only "Wireframe" cheat (Cheats → Gameplay). Draws depth-tested 3D geometry as polygon outlines via `glPolygonMode` in the fast3d GL backend; HUD/2D stay solid. Cheat state (`CHEAT_WIREFRAME`) is synced per-frame to the renderer global `gfx_wireframe_mode` in `bgTickPortals` (the `CHEAT_NOCULL` pattern). Read when touching `gfx_opengl_draw_triangles`/`gfx_opengl_set_depth_mode`, the `gfx_*` render-toggle globals, or the depth-test wireframe gate. GL-ES is a no-op (desktop-GL only). Live toggle: `/wireframe [on|off]` console command. **Documents the `bool`=`s32` (game) vs 1-byte (renderer) bridging gotcha** — read before sharing any flag between decompiled game code and the fast3d renderer.
- `docs/PORT_TONAL_INVERSION.md` — Port-only "Tonal Inversion" cheat (Cheats → Gameplay, `/tonal`): strict melodic inversion of all sequenced music — note pitches reflected around middle C in `__n_CSPHandleMIDIMsg` (`src/lib/naudio/n_csplayer.c`), SFX untouched. Cosmetic-only à la Mirror (ENABLED bank only, missions still save). **Documents the self-posted-note-off double-inversion trap** — read before touching the sequence player's note event handling. State: `g_SndTonalInversion` synced in `bgTickPortals`.
- `docs/PORT_MENU_COLOUR_SCHEMES.md` — Port-only menu colour-scheme architecture + **how to author hardcoded (compile-time, non-user-selectable) schemes**. The two colour layers: the palette tables (`g_MenuColours`/`g_MenuWave1Colours`/`g_MenuWave2Colours` in `menu.c`, indexed by `dialog->type`, `struct menucolourpalette` = 15 `0xRRGGBBAA` fields) and the hardcoded accents (`menuSchemeColour`, slider/dropdown/header). The two dispatch points (`menuApplyColourScheme` at `menuOpenDialog` + `dialogTick`; only DEFAULT is remapped). Recipe 1 = bake the active scheme as a compile-time constant + drop the dropdown/config; Recipe 2 = author a brand-new custom-hue palette row (append to all 3 tables + a new `MENUDIALOGTYPE_*`). Read when touching the menu palette, `menuApplyColourScheme`/`menuSchemeColour`, or replicating the selectable "Menu Colour Scheme" feature (`0406b9382`) as a baked-in one.
- `docs/PORT_OCTREE.md` — Port-only outdoor-room octree frustum-culling. Rooms flagged `ROOMFLAG_EX_OCTREE` (port-only `extra_flags` bit, separate from `ROOMFLAG_OUTDOORS`) build a per-room octree of the existing `vtxbatches[]` at load (`bgBuildRoomOctree`); each render pass traverses it, rejects offscreen subtrees, and emits a filtered copy of the leaf display list into `gfxAllocate` scratch (`bgEmitLeafCulled`) — copying state commands unconditionally, dropping only culled batches' `G_VTX`/`G_TRI*`. Read when touching `bgRenderRoomPass`'s LEAF case, `bgRenderRoomOpaque`/`Xlu`, `bgFindRoomVtxBatches`/`bgPopulateVtxBatchType`, or the per-frame gfx pools. Live toggle: `/octree [on|off|forcecull|stats]`. Non-octree path and N64 build are byte-identical.
- `docs/PORT_DLCACHE.md` — Port-only Fast3D "display-list cache": cache static room geometry in persistent GPU buffers and replay it with a GPU-side `uMVP` (added to every shader, identity for the immediate path), instead of CPU-transforming every vertex each frame. The enabling insight is that all CPU post-transform fixups (aspect-X, invert-Y, z-remap) are linear and fold into one matrix. New EXT opcodes `G_DLCACHE_BEGIN_EXT`/`END_EXT` (`gbiex.h`) bracket each room leaf (`bg.c` `bgRenderRoomPass`); the renderer keys the cache by the leaf `gdl` pointer, records object-space geometry on a miss (tee in `gfx_sp_tri1`/`gfx_sp_vertex`/`gfx_flush`), and replays per-segment in `dlcacheReplay`. Read when touching the fast3d vertex/flush hot path, the `uMVP` uniform, the `cache_*` rapi entries, or texture-cache invalidation (the cache is dropped on any texture-cache change so stored GL texture ids can't dangle). Live toggle: `/dlcache [on|off|stats|clear|ff]`. **Phase 1+2 done** (octree-aware: cached replay culls per vtxbatch via `g_BgCullVisible`, passed to the renderer in the `BEGIN` opcode), **plus GPU distance fog and dynamic vertex lighting** (shader-side palette) — 2-cycle/multitexture/grayscale/fog/dynamic-lighting all cached. Remaining roadmap coverage is the view-dependent cases only: `G_TEXTURE_GEN` (reflections), `G_LIGHTING` (normal lighting), and whole-room dyntex exclusion. Default off; N64 + `/dlcache off` byte-identical.
- `docs/PORT_DLCACHE_BLACK_TEXTURES.md` — **Open-issue ledger** for the dlcache "black / missing textures" family (2026-06-17 session). Two distinct **driver-specific** bugs on one user's 2023 GPU (dev couldn't repro either): **Bug A** (Vulkan/SDL_GPU cached walls culled / see-through) — **FIXED** as a per-machine cull-winding override (`Video.DlCacheFrontFace=ccw|cw`, the "DL Cache Flip Winding" Extended>Video checkbox, `Video.DlCacheCull`); **Bug B** (OpenGL base-game textures render black, geometry present) — **OPEN**, with the full elimination table (cull-immune, palette-off-immune, not the HD pack, `tex 104/4096` so not eviction, `BAD 0`) and the leading hypothesis (old GL driver + the dlcache VS texture sampler / `GL_MAX_VERTEX_TEXTURE_IMAGE_UNITS` not gated). Also the `/dlcache palette` and `/texcache` diagnostics added that day, and the **OG Xbox / NXDK-SDL2** porting notes (no SDL_GPU there; NV2A is fixed-function, so plan dlcache default-off + add the GL capability guards). Read before resuming the OpenGL black-texture hunt or porting fast3d to a low-end-GL target.
- `docs/PORT_GLARE_OCCLUSION.md` — Port-only fix so light glares (corona/bloom sprites, `artifact.c` → `func0f0b2150`) are occluded by the first-person weapon/hands instead of painting over them. Glares are depth-less 2D texrects whose visibility is a world-only LOS test (`artifactTestLos`), so a light behind the gun still emits one; the fix reorders `playerRenderHud` to draw `bgRenderArtifacts` **before** `bgunRender` (port-only `#ifndef PLATFORM_N64`), letting the opaque gun overdraw glares behind it (gun renders into a freshly-cleared depth buffer, so overdraw == depth-test). Read when touching the `playerRenderHud` first-person draw order, glare/artifact rendering, or before attempting a renderer-side texrect depth-test (fast3d draws rects at fixed `z=-1` and ignores prim-depth). N64 byte-identical.
- `docs/PORT_RAYTRACING.md` — Port-only **screen-space raytracing suite** (branch `port-raytracing`, BOTH backends): raymarched AO, directional sun shadows, SSR reflections, SSGI, and stochastic multi-bounce **path tracing** with per-player temporally-accumulated depth-validated reprojection. New `G_RTRESOLVE_EXT 0x4b` marker emitted at the top of `playerRenderHud` (world rendered, depth intact — bgunRender clears depth after, so the viewmodel is excluded by design) carrying a per-player `rtcamera` snapshot (`port/include/rt_ext.h`); pass ALGORITHMS shared between backends in `port/fast3d/gfx_rt_common.h` (declaration-free GLSL bodies; each backend prepends its own prelude) behind a nullable `rt_resolve` rapi entry. GL impl in `gfx_rt.cpp` (**runtime-CONFIRMED 2026-07-08**): dual capture path (format-matched RGB8/D24S8 blit = MSAA resolve, vs `glCopyTexSubImage2D` for the default framebuffer — depth blits demand format equality), full glGet state save/restore. SDL_GPU impl in `gfx_sdlgpu.cpp` RT section (**compile-verified only**): GLSL450 via the glslang/SPIRV-Cross pipeline (Vulkan + D3D12), fb depth sampled directly (SAMPLER usage added when `depth_samplable`), one scene-colour copy pass, std140 UBO with body-matching member names, uYSign=-1 top-down mapping, **requires MSAA off** (multisample depth unsamplable/unresolvable), must re-dirty `st.vs_dirty/fs_dirty` after (push-uniform slots clobbered). Composite is blended (mul AO×shadow + add GI+SSR) so MSAA edges survive on GL and HDR FP16 composes by construction. Includes the **dark/relight mode** (`/rt dark` + `/rt torch`): scene crushed to an ambient floor and re-lit by point lights harvested per-frame from the map's room lights (`rtCollectLights`, artifact.c — the glare data; shot-out lights extinguish), each with a screen-space shadow march, carried in the per-player `rtcamera` (nearest 24). Live control `/rt` (+ `/rt debug` buffer views — the diagnosis tool), config `Video.RT.*`, and an **Extended > Experiments > Raytracing (WIP)** menu page (warning-headed; `g_ExtendedRTMenuDialog` in `optionsmenu.c`). Read when touching `playerRenderHud`'s top, the rapi table tails, EXT opcode allocation (next free 0x4c), gfx_rt_common.h (edits hit BOTH backends — adding a uniform means touching both preludes), or any depth-reconstruction post-processing.
- `docs/PORT_MIRROR.md` — Port-only "Mirror" cheat (Cheats → Gameplay): flips the entire rendered 3D world left-right (a horizontal reflection) in the fast3d GL backend; works everywhere including the Carrington Institute hub (shared `lvRender`→`bgRender` path, no special-casing). 2D HUD/text stay un-mirrored. Purely cosmetic (gameplay/hit-detection run on un-mirrored coords) so it's net/save-safe. `CHEAT_MIRROR` syncs per-frame to renderer global `gfx_mirror_mode` in `bgTickPortals` (the `gfx_wireframe_mode` pattern). The flip is **negate clip-space X + compensate winding + reflect the per-room scissor** across BOTH render paths: immediate (`gfx_sp_vertex` X-negate, `gfx_sp_tri1` cross-negate — CPU cull) and dlcache replay (`uMVP` X-scale negate, `cache_set_cull` front-face flip — GPU cull); the portal draw-slot scissor is reflected about the viewport centre via `gfx_mirror_scissor_x`. Read when touching the fast3d vertex/tri/scissor path or the viewmodel. Live toggle: `/mirror [on|off]`. **Phase 2 (gun as a normal model in the left hand, not a mirror-image) is not yet implemented** — Phase 1 mirror-images the viewmodel along with the world. Default off; N64 byte-identical.
- `docs/PORTING_HOWTO.md` — Methodology guide for lifting any port-only feature in this repo into another fork / branch. Covers the decompilation contract, the two guard patterns, MPOPTION budget, protocol version discipline, wad versioning, deterministic invariants, helper choke points. Read once, then use as a checklist alongside the per-feature doc.
- `docs/PORT_COOP_DROPIN.md` — Port-only co-op drop-in (mid-mission join/leave/rejoin, proto 61): net co-op pre-allocates `NET_COOP_MAX_SLOTS` dormant player slots (`player.isdormant`), `SVC_COOP_CLAIM` seat/release, name-based reclaim, chr syncids in the co-op reconcile. Read when touching `netCoopEnterStage`/`netPlayersAllocate` binding, the all-out mission-fail loop in player.c, or the lv.c net render rollback (keys on the LOCAL player's order — claimants bind at wire slot N without the slot-0 swap).
- `docs/PORT_COOP_PLAN.md` — The **actionable** campaign co-op build plan (sequenced phases + concrete hook points). Locked decisions: one-level vertical slice, up to 4 players (built 2→4), checkpoint respawn. Identifies the linchpin NPC-AI client gate (`chrTick`→`chraTick`→`chraiExecute`, chr.c:2538 / chraction.c:13947), the host chr-state broadcast generalization (`g_MpBotChrPtrs`→`g_ChrSlots`), and the 2-vs-4 player-model risk (engine co-op is 2-player). Read with PORT_COOP_PREP.md (design rationale).
- `docs/PORT_COOP_PREP.md` — Pre-plan design notes for **campaign co-op** over the existing netplay. Inventories the chr-replication stack that Combat-Sim sim sync already provides (position interp, anim cross-fade, room time-align, vertical grounding, HP/shield) and enumerates what co-op adds: non-`aibot` NPC AI gating, the `actiontype`/union problem at campaign scope, objective/script/stage-flow sync, the `chr->aibot`-gated net logic that must widen to all client-driven chrs, and a recommended first vertical slice. Read before scoping or planning co-op work.

## Submodules / do-not-modify paths

- `tools/recomp` — submodule pointing at [Emill/ido-static-recomp](https://github.com/Emill/ido-static-recomp.git). Do not commit edits inside this path; it tracks upstream independently.
- `port/external/` — bundled third-party sources (ENet, minimp3, GLAD). Treat as vendored; upstream patches go in commit messages, not local edits without documentation.
- `port/fast3d/` — libultraship fast3d renderer carried in-tree. No netplay modifications; treat changes here as renderer work, not port work.
